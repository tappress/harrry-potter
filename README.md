# Harry Potter Word Frequency Analyzer

## Пропозиції щодо оптимізації пам'яті (Memory Optimization)

### 1. Використати BufferedReader замість Files.readAllBytes()
Потокове читання файлу символ за символом замість завантаження всього файлу в пам'ять. Для файлу 442 KB економить ~400 KB RAM.

### 2. Обробляти текст посимвольно без створення String[]
Замість `content.split(" +")`, який створює масив з 400 000 елементів, обробляти кожен символ окремо та формувати слова на льоту.

### 3. Використати int[] замість Long для підрахунку частоти
`Map<String, int[]>` замість `Map<String, Long>` уникає boxing/unboxing та економить ~8 байт на кожен запис.

### 4. Використати PriorityQueue для топ-K елементів
Замість сортування всієї колекції O(n log n), використати min-heap розміром K для знаходження топ-K за O(n log K).

### 5. Попередньо виділити розмір HashMap
`new HashMap<>(10000)` зменшує кількість rehashing операцій та фрагментацію пам'яті.

### 6. Використати StringBuilder з фіксованою ємністю
`new StringBuilder(50)` для слів замість динамічного розширення буфера.

### 7. Очищати StringBuilder через setLength(0)
Повторне використання того ж StringBuilder замість створення нових об'єктів для кожного слова.

### 8. Уникати створення проміжних колекцій
Видалити `distinctString`, `String[] distincts`, `int[] freq` - замінити на один прохід з HashMap.

### 9. Використати try-with-resources для автоматичного закриття ресурсів
Гарантує звільнення пам'яті буферів після завершення читання.

### 10. Використати примітивні типи де можливо
`int ch` замість `Character`, `char` замість `String` для одиночних символів.

### 11. Видалити проміжні String об'єкти
Уникати `word + " " + count` в циклі - створювати рядки тільки при фінальному виведенні.

### 12. Використати ISO_8859_1 кодування для ASCII тексту
Один байт на символ замість UTF-8 декодування для англійського тексту.

### 13. Вимірювати використання пам'яті через Runtime
`Runtime.getRuntime().totalMemory() - freeMemory()` для моніторингу споживання.

### 14. Викликати System.gc() перед вимірюванням
Для отримання точніших результатів вимірювання пам'яті.

### 15. Використати char[] замість String для тимчасових даних
Масиви символів мають менший overhead ніж String об'єкти.

---

## Порівняння продуктивності

| Версія | Час виконання | Пам'ять |
|--------|---------------|---------|
| Оригінальна (O(n²)) | ~2000+ мс | Висока |
| Рефакторинг (Streams) | 108 мс | Середня |
| Memory Optimized | 39 мс | 73 KB |

**Покращення**: швидкість зросла в ~2.8 рази порівняно з рефакторингом.

